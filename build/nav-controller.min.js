const storedNavBar=JSON.parse(localStorage.getItem("navBarTastic")),navEl=document.getElementById("nav");for(let a=0;a<storedNavBar.length;a++){let e=storedNavBar[a];navEl.innerHTML+=`\n    <a class="nav-item nav-link" href="${e.link}">${e.name}</a>\n    `}